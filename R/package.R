#' tsn: Time-Series Network Construction
#'
#' `tsn` constructs distance, visibility, and state-transition networks from
#' time-series data. [tsn()] is the core network constructor; [vg()],
#' [discretize()], [trend()], and the `ts_tna()` family provide focused
#' workflows. Core construction uses only packages distributed with R, while
#' transition-network models require the suggested Nestimate package.
#'
#' @keywords internal
"_PACKAGE"
