% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/object.R
\name{as.data.frame.ts_tna}
\alias{as.data.frame.ts_tna}
\title{Coerce a transition network to a tidy edge table}
\usage{
\method{as.data.frame}{ts_tna}(
  x,
  row.names = NULL,
  optional = FALSE,
  what = c("edges", "series"),
  ...
)
}
\arguments{
\item{x}{A \code{ts_tna} result.}

\item{row.names}{Optional row names.}

\item{optional}{Ignored.}

\item{what}{Which table to return: \code{"edges"} (one row per state pair, the
default) or \code{"series"} (the tidy per-observation source table with \code{id},
\code{time}, \code{value}, and \code{state}).}

\item{...}{Ignored.}
}
\value{
A base data frame.
}
\description{
Returns one row per state pair, so a transition network can be read, sorted,
or joined as data rather than reached into. \code{as.data.frame()} on the
underlying Nestimate object errors, so this method supplies the tidy view.
}
\examples{
\dontshow{if (requireNamespace("Nestimate", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
network <- ts_tna(cumsum(rnorm(60)), labels = c("low", "mid", "high"))
as.data.frame(network)
head(as.data.frame(network, what = "series"))
\dontshow{\}) # examplesIf}
}
