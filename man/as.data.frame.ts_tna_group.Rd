% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/object.R
\name{as.data.frame.ts_tna_group}
\alias{as.data.frame.ts_tna_group}
\title{Coerce a grouped transition network to a tidy data frame}
\usage{
\method{as.data.frame}{ts_tna_group}(
  x,
  row.names = NULL,
  optional = FALSE,
  what = c("edges", "series", "groups"),
  ...
)
}
\arguments{
\item{x}{A \code{ts_tna_group} result from \code{\link[=ts_tna]{ts_tna()}} and friends, built with
their \code{group} argument.}

\item{row.names}{Optional row names.}

\item{optional}{Ignored.}

\item{what}{Which table to return: \code{"edges"} (one row per group and state
pair, the default), \code{"series"} (the per-observation source table for every
group), or \code{"groups"} (a one-row-per-group index of how much data backs
each network).}

\item{...}{Ignored.}
}
\value{
A base data frame whose first column is \code{group}.
}
\description{
A grouped model is a collection of networks, one per group. This method is
the tidy view of it: every table it returns carries a \code{group} column, so a
comparison across groups is a data frame you can read, sort, or join rather
than a set of objects you have to reach into one at a time.
}
\examples{
\dontshow{if (requireNamespace("Nestimate", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(esm_srl)
networks <- ts_tna(
  subset(esm_srl, !is.na(effort)),
  value = "effort", id = "name", time = "occasion",
  group = "day_type", labels = c("low", "mid", "high")
)
as.data.frame(networks, what = "groups")
head(as.data.frame(networks))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=ts_tna]{ts_tna()}} for the \code{group} argument that builds these models.
}
