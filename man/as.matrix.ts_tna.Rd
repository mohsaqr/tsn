% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/object.R
\name{as.matrix.ts_tna}
\alias{as.matrix.ts_tna}
\title{Coerce a transition network to a weighted adjacency matrix}
\usage{
\method{as.matrix}{ts_tna}(x, rownames.force = NA, ...)
}
\arguments{
\item{x}{A \code{ts_tna} result.}

\item{rownames.force}{Ignored.}

\item{...}{Ignored.}
}
\value{
A named numeric matrix, states in row and column order.
}
\description{
The state-to-state weight matrix behind a \code{\link[=ts_tna]{ts_tna()}}-family model: transition
probabilities for \code{ts_tna()}, counts for \code{ts_ftna()}, and so on. Without this
method \code{as.matrix()} falls through to the default, which coerces the object's
component list rather than its network.
}
\examples{
\dontshow{if (requireNamespace("Nestimate", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
network <- ts_tna(cumsum(rnorm(60)), labels = c("low", "mid", "high"))
as.matrix(network)
\dontshow{\}) # examplesIf}
}
