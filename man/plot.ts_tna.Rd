% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stna.R
\name{plot.ts_tna}
\alias{plot.ts_tna}
\title{Plot a Time-Series Transition Network}
\usage{
\method{plot}{ts_tna}(
  x,
  type = c("combined", "network", "series"),
  series = NULL,
  max_series = 3L,
  network = c("per_series", "summary"),
  overlay = c("vertical", "none"),
  ribbon = FALSE,
  points = FALSE,
  node_size = c("instrength", "outstrength", "strength"),
  node_scale = 1,
  network_width = 0.85,
  show_weights = TRUE,
  alpha = 0.28,
  line_color = "#3B4252",
  line_width = 1.5,
  point_size = 0.9,
  strip_height = 0.09,
  palette = NULL,
  legend = TRUE,
  xlab = "Time",
  ylab = "Value",
  cex = 1,
  grid = TRUE,
  background = "#FFFFFF",
  ...
)
}
\arguments{
\item{x}{A \code{ts_tna} result from \code{\link[=ts_tna]{ts_tna()}}, \code{\link[=ts_ftna]{ts_ftna()}}, \code{\link[=ts_cna]{ts_cna()}}, or
\code{\link[=ts_atna]{ts_atna()}}.}

\item{type}{\code{"combined"}, \code{"network"}, or \code{"series"}.}

\item{series}{Optional series IDs to draw (default: up to \code{max_series}).}

\item{max_series}{Maximum number of series panels (default \code{3}).}

\item{network}{Which network(s) to draw: \code{"per_series"} (default) —
one network per series, each built from that series' own transitions
— or \code{"summary"} — the single pooled model across all series.}

\item{overlay}{Series shading: \code{"vertical"} (default) or \code{"none"}.}

\item{ribbon}{Whether to run the state strip under each series panel.}

\item{points}{Whether to draw state-coloured observation points.}

\item{node_size}{Node sizing rule: \code{"instrength"} (default),
\code{"outstrength"}, or \code{"strength"} — passed to cograph as
\code{scale_nodes_by} (self-loops excluded from the centrality).}

\item{node_scale}{Multiplier for cograph's node size range
(\code{node_size_range = c(7, 14) * node_scale}).}

\item{network_width}{Width of the network column relative to the series
column (default \code{0.85}); combined view only.}

\item{show_weights}{Whether to print edge weights on the network.}

\item{alpha}{Shading opacity.}

\item{line_color, line_width, point_size}{Series styling.}

\item{strip_height}{Ribbon strip height as a fraction of each panel.}

\item{palette}{State colours (preset name, named vector, or vector).}

\item{legend}{Whether to draw the state legend row.}

\item{xlab, ylab}{Series axis titles.}

\item{cex}{Global text size multiplier.}

\item{grid}{Whether to draw the background grid.}

\item{background}{Panel background colour.}

\item{...}{Forwarded to \code{cograph::splot()} for the network panel
(e.g. \code{scale_nodes_by}, \code{node_size_range}, \code{scale_nodes_scale},
\code{edge_color}); user values override the defaults set here.}
}
\value{
\code{x}, invisibly.
}
\description{
Combined views of a \code{ts_tna()}-family result. \code{type = "combined"}
(default) places each source series — with optional state shading
and/or a classification ribbon — beside a transition network on the
same row. With \code{network = "per_series"} (default) every row pairs a
series with \strong{its own} network (rebuilt via \code{\link[=series_networks]{series_networks()}}, so
all networks share the same node set and are directly comparable);
\code{network = "summary"} draws the pooled model instead, spanning all
series rows and titled "Summary". One state colour mapping ties the
shading, ribbons, and network nodes together. \code{type = "network"} draws
only the network(s) — a row of per-series networks or the single
summary; \code{type = "series"} only the series panels. Every combination
of \code{overlay} (shaded or not) and \code{ribbon} is available.
}
\details{
Network panels are rendered exclusively by
\code{cograph::splot()} — the full TNA style with self-loops and probability
labels — with node fills matching the state shading and nodes sized by
cograph's native centrality scaling (\code{scale_nodes_by = "instrength"} by
default, with \code{loops = FALSE}), so heavily-entered states read as larger
and self-transitions do not drown the between-state structure; pass
\code{scale_nodes_by = list("instrength", loops = TRUE)} through \code{...} to
include self-transitions. \code{"outstrength"} and \code{"strength"}
(total) are alternatives, and \code{...} is forwarded to \code{cograph::splot()}
for full control (anything you set there overrides these defaults).
\code{show_weights} controls cograph's edge labels.
}
\examples{
\dontshow{if (requireNamespace("Nestimate", quietly = TRUE) && requireNamespace("cograph", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
series <- list(a = cumsum(rnorm(80)), b = cumsum(rnorm(80)))
network <- ts_tna(series, n_states = 3, labels = c("low", "mid", "high"))
plot(network)
plot(network, network = "summary")
plot(network, ribbon = TRUE, overlay = "none")
plot(network, "network")
\dontshow{\}) # examplesIf}
}
