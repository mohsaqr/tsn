% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stna.R
\name{plot.ts_tna_group}
\alias{plot.ts_tna_group}
\title{Plot One Network of a Grouped Transition Model}
\usage{
\method{plot}{ts_tna_group}(x, y = NULL, group = NULL, ...)
}
\arguments{
\item{x}{A \code{ts_tna_group} result from \code{\link[=ts_tna]{ts_tna()}} and friends, built with
their \code{group} argument.}

\item{y}{Ignored.}

\item{group}{Group label to draw. It may be omitted when the model holds
exactly one group.}

\item{...}{Passed to \code{\link[=plot.ts_tna]{plot.ts_tna()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
A grouped model holds one complete \code{ts_tna} network per group, and there is
no single picture of all of them, so one group is drawn at a time. The
selected network is rendered by \code{\link[=plot.ts_tna]{plot.ts_tna()}} with its full argument
surface — combined series-plus-network views, state shading, ribbons, and
every \code{cograph::splot()} option.
}
\examples{
\dontshow{if (requireNamespace("Nestimate", quietly = TRUE) && requireNamespace("cograph", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(esm_srl)
networks <- ts_tna(
  subset(esm_srl, !is.na(effort)),
  value = "effort", id = "name", time = "occasion",
  group = "day_type", labels = c("low", "mid", "high")
)
plot(networks, group = "weekday")
plot(networks, group = "weekend", type = "network")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=ts_tna]{ts_tna()}} for the \code{group} argument;
\code{\link[=as.data.frame.ts_tna_group]{as.data.frame.ts_tna_group()}} for the tidy views of the collection.
}
