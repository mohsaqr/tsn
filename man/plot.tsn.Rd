% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.tsn}
\alias{plot.tsn}
\title{Plot a TSN Result}
\usage{
\method{plot}{tsn}(x, type = c("network", "series"), ...)
}
\arguments{
\item{x}{A \code{tsn} result.}

\item{type}{What to draw: \code{"network"} (default) or \code{"series"}.}

\item{...}{Arguments forwarded to the network or series renderer.
See details.}
}
\value{
\code{x}, invisibly.
}
\description{
By default, \code{plot()} renders the constructed network through
\code{\link[cograph:splot]{cograph::splot()}}. The diagnostic source-series view (with optional state
shading) remains available via \code{type = "series"} and does not require
cograph.
}
\details{
Network rendering belongs exclusively to cograph. tsn supplies restrained
defaults that keep edges visible and avoid meaningless per-node rainbow
fills; named arguments in \code{...} override any of them. tsn results already
implement the \code{cograph_network} contract. Source-series views remain
tsn-specific because they visualize the original observations rather than a
generic network.

For \code{type = "network"}, use cograph arguments such as \code{layout}, \code{labels},
\code{scale_nodes_by}, \code{node_size_range}, and \code{edge_label_style}. For
\code{type = "series"}, \code{...} accepts \code{series},
\code{overlay}, \code{points}, \code{trend}, \code{columns}, \code{max_series}, \code{scales}, and
\code{palette}.
}
\examples{
network <- tsn(c(3, 1, 4, 2, 5, 3, 6, 2, 7), "hvg")
if (requireNamespace("cograph", quietly = TRUE)) {
  plot(network)
  plot(network, layout = "spring")
}

data(srl)
states <- tsn(
  srl,
  value = "effort",
  id = "name",
  time = "day",
  series = "Erik",
  unit = "state",
  discretization = "quantile"
)
plot(states, "series", overlay = "vertical")
}
