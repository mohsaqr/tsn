% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discretize.R
\name{plot.tsn_states}
\alias{plot.tsn_states}
\title{Plot a state discretization}
\usage{
\method{plot}{tsn_states}(
  x,
  type = c("overlay", "ribbon", "heatmap", "stack"),
  series = NULL,
  overlay = c("horizontal", "vertical", "none"),
  lines = "none",
  min_run = 1L,
  points = TRUE,
  max_series = NULL,
  columns = NULL,
  palette = NULL,
  alpha = NULL,
  line_color = "#3B4252",
  line_width = 1.6,
  point_size = 1,
  strip_height = 0.1,
  sort = "none",
  border = FALSE,
  with = NULL,
  with_label = "Series",
  shade = TRUE,
  color_line = FALSE,
  ribbons = NULL,
  ribbon_label = "States",
  legend = TRUE,
  xlab = "Time",
  ylab = "Value",
  cex = 1,
  grid = TRUE,
  background = "#FFFFFF",
  ...
)
}
\arguments{
\item{x}{A \code{tsn_states} result.}

\item{type}{The view: \code{"overlay"}, \code{"ribbon"}, \code{"heatmap"}, or \code{"stack"}.
The stack view draws a companion series (\code{with}) above the discretized
series with BOTH panels vertically shaded by the discretized states —
e.g. the original time series read against the states of its rolling
complexity.}

\item{series}{Optional character vector of series IDs to display.}

\item{overlay}{Colour shading for the overlay view: \code{"horizontal"} (default;
value bands), \code{"vertical"} (time runs), or \code{"none"}.}

\item{lines}{Dashed guide lines complementing the shading:
\code{"horizontal"} draws grey lines at the state boundaries (the
discretization breaks when available, empirical boundaries otherwise).
Transformed and magnitude-space breaks are converted back to the raw
signed value axis; adaptive local boundaries are omitted.
\code{"vertical"} draws a line at each state transition (filtered by
\code{min_run}) \strong{in the colour of the state that starts there} — a green
dashed line means the green state begins at that point and runs until
the next line; \code{"both"}, or \code{"none"} (default). Combine one shading
direction with dashed lines in the other, e.g.
\verb{overlay = "horizontal", lines = "vertical"}.}

\item{min_run}{For \code{lines = "vertical"}: only transitions into a state
persisting at least \code{min_run} observations get a line.}

\item{points}{Whether to draw observations over each line.}

\item{max_series}{Maximum number of series to draw (\code{25} for the heatmap,
\code{10} otherwise).}

\item{columns}{Number of panel columns.}

\item{palette}{State colours: \code{NULL} for the default palette, a preset name
(\code{"default"}, \code{"okabe"}, \code{"viridis"}, \code{"cool"}, \code{"warm"}, \code{"pastel"},
\code{"dark"}), a named colour vector keyed by state, or a plain colour
vector.}

\item{alpha}{Shading opacity for the overlay view.}

\item{line_color}{Series line colour.}

\item{line_width}{Series line width.}

\item{point_size}{Observation point size.}

\item{strip_height}{Height of the ribbon strip as a fraction of the panel.}

\item{sort}{Heatmap row order: \code{"none"}, \code{"mean"} (by series mean value),
or \code{"state"} (group rows by modal state).}

\item{border}{Whether heatmap tiles carry a thin separator border.}

\item{with}{For \code{type = "stack"}: a finite numeric vector aligned with the
rows of \code{x} giving the companion series (e.g. the original values when
\code{x} discretizes a derived series).}

\item{with_label}{Y-axis title of the companion panel.}

\item{shade}{For \code{type = "stack"}: whether to draw the vertical state
shading on both panels.}

\item{color_line}{For \code{type = "stack"}: colour each line segment by the
state at its left endpoint instead of drawing a neutral line.}

\item{ribbons}{For \code{type = "ribbon"}: an optional NAMED list of additional
classifications to stack below the series — each element a
\code{tsn_states}, a \code{\link[=trend]{trend()}} result, or a character/factor vector aligned
with the selected series. The object's own states form the first strip
(labelled \code{ribbon_label}); one series at a time. This is the
resilience-style multi-ribbon view: the series read against several
classifications at once, each strip labelled on the axis with its own
legend row.}

\item{ribbon_label}{Strip label for the object's own states in the
multi-ribbon view.}

\item{legend}{Whether to draw the legend row.}

\item{xlab, ylab}{Axis titles.}

\item{cex}{Global text size multiplier.}

\item{grid}{Whether to draw the background grid.}

\item{background}{Panel background colour.}

\item{...}{Reserved for future options.}
}
\value{
\code{x}, invisibly.
}
\description{
Four views of a discretized series, selected with \code{type}. \code{"overlay"}
(default) draws each series with translucent state shading behind it;
\code{"ribbon"} keeps the series clean and runs a state-classification strip
underneath it instead; \code{"heatmap"} draws one row per series with one
coloured tile per observation, which scales to many series where the
faceted views do not; \code{"stack"} places an aligned companion series above
the discretized series.
}
\examples{
data(srl)
complete <- subset(srl, !is.na(effort))
states <- discretize(
  complete,
  value = "effort", id = "name", time = "day",
  method = "quantile", n_states = 3
)
plot(states, series = "Erik")
plot(states, series = "Erik", overlay = "vertical", lines = "horizontal")
plot(states, "ribbon", series = "Erik", points = FALSE)
plot(states, "heatmap", max_series = 12, sort = "mean")
plot(states, "heatmap", palette = "viridis", max_series = 12)

# Stack: original series read against the states of its rolling volatility.
one <- subset(complete, name == "Erik")
volatility <- abs(c(0, diff(one$effort)))
vol_states <- discretize(
  data.frame(name = one$name, day = one$day, vol = volatility),
  value = "vol", id = "name", time = "day",
  method = "quantile", n_states = 3
)
plot(vol_states, "stack", with = one$effort, with_label = "Effort",
     ylab = "Volatility")
}
