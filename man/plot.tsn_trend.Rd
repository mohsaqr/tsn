% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend.R
\name{plot.tsn_trend}
\alias{plot.tsn_trend}
\title{Plot a trend classification}
\usage{
\method{plot}{tsn_trend}(
  x,
  type = c("points", "ribbon", "heatmap", "panels"),
  series = NULL,
  max_series = NULL,
  columns = NULL,
  palette = NULL,
  line_color = "#9AA3AD",
  line_width = 1.3,
  point_size = 1.05,
  strip_height = 0.1,
  sort = "none",
  border = FALSE,
  flat_band = TRUE,
  legend = TRUE,
  xlab = "Time",
  ylab = "Value",
  cex = 1,
  grid = TRUE,
  background = "#FFFFFF",
  ...
)
}
\arguments{
\item{x}{A \code{tsn_trend} result.}

\item{type}{The view: \code{"points"}, \code{"ribbon"}, \code{"heatmap"}, or \code{"panels"}.}

\item{series}{Optional character vector of series IDs to display.}

\item{max_series}{Maximum number of series to draw (\code{25} for the heatmap,
\code{4} for the panel view, \code{10} otherwise).}

\item{columns}{Number of panel columns.}

\item{palette}{Optional named colours overriding individual trend states
(e.g. \code{c(Ascending = "forestgreen")}).}

\item{line_color}{Series line colour.}

\item{line_width}{Series line width.}

\item{point_size}{Observation point size.}

\item{strip_height}{Ribbon strip height as a fraction of the panel.}

\item{sort}{Heatmap row order: \code{"none"}, \code{"mean"}, or \code{"state"}.}

\item{border}{Whether heatmap tiles carry a thin separator border.}

\item{flat_band}{For \code{type = "panels"}, shade the \code{epsilon} flat band on
the metric panel so the classification threshold is visible.}

\item{legend}{Whether to draw the legend row.}

\item{xlab, ylab}{Axis titles.}

\item{cex}{Global text size multiplier.}

\item{grid}{Whether to draw the background grid.}

\item{background}{Panel background colour.}

\item{...}{Reserved for future options.}
}
\value{
\code{x}, invisibly.
}
\description{
Four views of a trend classification, selected with \code{type}. \code{"points"}
(default) draws each series with observations coloured by trend state;
\code{"ribbon"} runs the trend classification as a strip underneath the clean
series line; \code{"heatmap"} draws one row per series with one coloured tile
per observation, comparing trend timing across many series; \code{"panels"}
stacks each series above its rolling trend metric (the slope or
correlation \code{trend()} classified on) so a state change can be read
against the metric that produced it.
}
\examples{
data(srl)
complete <- subset(srl, !is.na(effort))
classified <- trend(
  complete,
  value = "effort", id = "name", time = "day", window = 7
)
plot(classified, series = "Erik")
plot(classified, "ribbon", series = "Erik")
plot(classified, "panels", series = "Erik")
plot(classified, "heatmap", max_series = 12)
}
