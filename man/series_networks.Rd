% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stna.R
\name{series_networks}
\alias{series_networks}
\title{Per-Series Transition Networks}
\usage{
series_networks(x, series = NULL)
}
\arguments{
\item{x}{A \code{ts_tna} result from \code{\link[=ts_tna]{ts_tna()}}, \code{\link[=ts_ftna]{ts_ftna()}}, \code{\link[=ts_cna]{ts_cna()}}, or
\code{\link[=ts_atna]{ts_atna()}}.}

\item{series}{Optional series IDs to keep (default: all).}
}
\value{
A named \code{tsn_series_networks} collection containing one \code{ts_tna}
object per series. Its print, summary, and data-frame methods return a tidy
one-row-per-series index; \code{plot()} draws the sole model or a model selected
with its \code{series} argument.
}
\description{
Split a pooled \code{ts_tna()}-family model into one full model per source
series. Each element is rebuilt with the same network type and the
shared state alphabet (\code{params = list(alphabet = ...)}), so every
network has the same node set in the same order — directly comparable
with each other and with the pooled summary model. Each element is a
complete \code{ts_tna} netobject: it keeps its own series data, plots with
\code{\link[=plot.ts_tna]{plot.ts_tna()}}, and works with compatible Nestimate verbs. Statistical
procedures still require enough independent sequences for their sampling
design; a one-series split is descriptive rather than bootstrap-ready.
}
\examples{
\dontshow{if (requireNamespace("Nestimate", quietly = TRUE) && requireNamespace("cograph", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
series <- list(a = cumsum(rnorm(80)), b = cumsum(rnorm(80)))
pooled <- ts_tna(series, n_states = 3, labels = c("low", "mid", "high"))
networks <- series_networks(pooled)
plot(networks, series = "a")
\dontshow{\}) # examplesIf}
}
