% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend.R
\name{trend}
\alias{trend}
\title{Classify Rolling Trends in a Time Series}
\usage{
trend(
  data,
  value = NULL,
  id = NULL,
  time = NULL,
  series = NULL,
  window = NULL,
  method = "slope",
  slope = "robust",
  epsilon = 0.05,
  turbulence_threshold = 5,
  flat_to_turbulent_factor = 1.5,
  align = "center"
)
}
\arguments{
\item{data}{A numeric vector, \code{ts}, matrix, named list of numeric vectors,
data frame, or a \code{tsn} object (its source series are used).}

\item{value}{Optional value-column name for long data.}

\item{id}{Optional series-ID column name for long data.}

\item{time}{Optional time-column name for long data.}

\item{series}{Optional series IDs or wide-data column names to select.}

\item{window}{Rolling-window width. When \code{NULL}, the adaptive default
\code{max(3, min(n, round(n / 10)))} is used, where \code{n} is the shortest series
length. Must satisfy \verb{2 < window < n}.}

\item{method}{Trend metric: \code{"slope"} (default) or \code{"growth_factor"}.}

\item{slope}{Slope estimator when \code{method = "slope"}: \code{"robust"} (Theil-Sen,
the default), \code{"ols"}, \code{"spearman"}, or \code{"kendall"}.}

\item{epsilon}{Flat-band half-width. Default \code{0.05}.}

\item{turbulence_threshold}{Baseline volatility threshold for the turbulent
override. Default \code{5}.}

\item{flat_to_turbulent_factor}{Multiplier applied to \code{turbulence_threshold}
for points already classified as flat. Default \code{1.5}.}

\item{align}{Window alignment: \code{"center"} (default), \code{"right"}, or \code{"left"}.
The metric is assigned to the centre, rightmost, or leftmost point of the
window respectively.}
}
\value{
A tidy data frame of class \code{tsn_trend} with one row per observation
and columns \code{id}, \code{time}, \code{value}, \code{metric}, and \code{state} (a factor over the
six trend classes). Print, summary, and plot methods are provided.
}
\description{
\code{trend()} computes a rolling trend metric for each observation and classifies
every point as \code{"Ascending"}, \code{"Descending"}, \code{"Flat"}, \code{"Turbulent"},
\code{"Missing Data"}, or \code{"Initial"}. It is a faithful base-R port of the
upstream \code{tsn::trend()} behaviour: rolling slope (OLS, Theil-Sen, Spearman,
or Kendall) or growth-factor metrics, an \code{epsilon} flat band, and a
volatility override that reclassifies noisy segments as turbulent.
}
\details{
The metric is first thresholded with \code{epsilon}: values above \code{+epsilon}
(or above \code{1 + epsilon} for growth factors) are ascending, values below
\code{-epsilon} (or \code{1 - epsilon}) are descending, and the remainder are flat.
A rolling volatility measure (coefficient of variation plus half the range
factor of the metric) then overrides these labels with \code{"Turbulent"} when it
exceeds \code{turbulence_threshold}. Segments already labelled \code{"Flat"} use the
higher threshold \code{turbulence_threshold * flat_to_turbulent_factor}, making
them more resistant to noise-driven reclassification.
}
\examples{
set.seed(123)
walk <- cumsum(rnorm(120))
trend(walk, window = 15, slope = "ols", epsilon = 0.1)

trend(c(1, 2, 3, 4, 5, 4, 3, 2, 1), window = 3, method = "growth_factor")
}
