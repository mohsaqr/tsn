% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vg.R
\name{vg}
\alias{vg}
\title{Build a Visibility Graph}
\usage{
vg(data, type = c("natural", "horizontal"), ...)
}
\arguments{
\item{data}{A numeric vector, \code{ts}, numeric matrix, named list of numeric
vectors, or data frame (the same inputs \code{\link[=tsn]{tsn()}} accepts).}

\item{type}{Visibility rule: \code{"natural"} (the natural visibility graph, the
default) or \code{"horizontal"} (the horizontal visibility graph).}

\item{...}{Any other \code{\link[=tsn]{tsn()}} argument relevant to visibility networks, for
example \code{value}, \code{id}, \code{time}, \code{series}, \code{unit}, \code{state}, \code{discretization},
\code{n_states}, \code{m}, \code{tau}, \code{directed}, \code{limit}, \code{penetrable}, \code{decay},
\code{aggregation}, and \code{seed}.}
}
\value{
A tidy \code{tsn} network object (also a \code{netobject} and a
\code{cograph_network}), equivalent to the corresponding \code{\link[=tsn]{tsn()}} call. The
stored provenance call records \code{vg()} rather than \code{tsn()}.
}
\description{
\code{vg()} is a discoverable verb for the package's flagship construction, the
visibility graph. It is a thin wrapper around \code{\link[=tsn]{tsn()}} with
\code{method = "visibility"}: \code{vg(x)} is equivalent to \code{tsn(x, "nvg")} and
\code{vg(x, "horizontal")} is equivalent to \code{tsn(x, "hvg")}. Every input shape
and every visibility-relevant argument that \code{\link[=tsn]{tsn()}} accepts is forwarded
through \code{...}, and the result is the same dual-class \code{tsn}/\code{netobject}/
\code{cograph_network} object.
}
\examples{
vg(c(3, 1, 4, 2, 5, 3, 6, 2, 7))
vg(c(3, 1, 4, 2, 5, 3, 6, 2, 7), "horizontal")

network <- vg(c(3, 1, 4, 2, 5, 3, 6, 2, 7), "horizontal", directed = TRUE)
if (requireNamespace("cograph", quietly = TRUE)) {
  plot(network)
}

data(srl)
states <- vg(
  srl,
  value = "effort",
  id = "name",
  time = "day",
  series = "Erik",
  unit = "state",
  discretization = "quantile"
)
plot(states, "series", overlay = "vertical")
}
\seealso{
\code{\link[=tsn]{tsn()}} for the full argument set and the distance-network family.
}
